package com.tanquoc73.be.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class UserController {
    // This controller can be used for additional user-related endpoints
    // The /api/user/me endpoint is handled by AuthController
}
